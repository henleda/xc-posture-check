# engine-strict is intentionally OFF so local dev on Node 25 doesn't block
# while Vercel builds on Node 20 (per engines.node in package.json).
# Remove this comment and flip to true once local Node 20 is installed.
engine-strict=false
auto-install-peers=true
strict-peer-dependencies=false
